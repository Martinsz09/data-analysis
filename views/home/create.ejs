<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Data Analysis</title>
  <link rel="stylesheet" href="/css/home/create.css">
</head>
<body>

  <nav>
    <h2>Menu</h2>
    <ul>
      <li><a href="/home/create">Criar gráfico</a></li>
      <li><a href="/home/view">Visualizar gráficos</a></li>
      <li><a href="/home/graphics">Graficos</a></li>
    </ul>
  </nav>

  <main>

       <div class="form-wrapper">
      <% if (typeof errorMessage !== 'undefined') { %>
        <div class="error-message"><%= errorMessage %></div>
      <% } %>
    <form action="/home/create" method="POST" id="createChartForm">
      <label for="title">Título do gráfico *</label>
      <input type="text" id="title" name="title" placeholder="Ex: Vendas Mensais" required />

      <label for="type">Tipo de gráfico *</label>
      <select id="type" name="type" required>
        <option value="" disabled selected>Selecione o tipo</option>
        <option value="bar">Barra</option>
        <option value="line">Linha</option>
        <option value="pie">Pizza</option>
        <option value="doughnut">Rosca</option>
        <option value="radar">Radar</option>
      </select>

      <label for="labels">Labels (rótulos) *</label>
      <input type="text" id="labels" name="labels" placeholder="Ex: Janeiro,Fevereiro,Março" required />
      <small>Digite os rótulos separados por vírgula</small>

      <label for="values">Valores dos dados *</label>
      <input type="text" id="values" name="values" placeholder="Ex: 10,20,30" required />
      <small>Digite os valores separados por vírgula (números)</small>

      <label for="datasetLabel">Nome do conjunto de dados *</label>
      <input type="text" id="datasetLabel" name="datasetLabel" placeholder="Ex: Vendas" required />

      <button type="submit">Criar Gráfico</button>
    </form>
  </main>

</body>
</html>
